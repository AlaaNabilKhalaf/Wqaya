import 'package:flutter/material.dart';

class ExistViewBody extends StatelessWidget {
  const ExistViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
